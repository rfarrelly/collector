[project]
name = "collector"
version = "0.1.0"
description = "Source-agnostic football data collection"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.0",
    "pydantic>=2.12.5",
    "pyyaml>=6.0.3",
]
